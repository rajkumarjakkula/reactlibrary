const mongoose = require('mongoose')
const allBookSchema = new mongoose.Schema({
    title:{
        type:String,
        required:true
    },
    body:{
        type:String,
        required:true
    },
    photo:{
        type:String,
        required:true
    },
})

mongoose.model("Allbooks",allBookSchema)